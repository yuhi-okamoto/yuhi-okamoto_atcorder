const main = input =>{
  const args = input.split("\n");
  const s = args[0];
  let result = "";
  while(result.length < 6){
    result = result + s;
  }
  console.log(result)
}

main(require("fs").readFileSync("/dev/stdin", "utf8"));
